# summary dashboard
cnodes_load:
    yMin: 0
    yMax: 16
    hideLegend: false
    target:
    - 'collectd.cnode*-m_{domain}.load.load.shortterm'

cnodes_cpu_idle:
    yMin: 0
    yMax: 100
    hideLegend: false
    target:
    - 'averageSeries(collectd.cnode01-m_{domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.cnode02-m_{domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.cnode03-m_{domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.cnode04-m_{domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.cnode05-m_{domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.cnode06-m_{domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.cnode07-m_{domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.cnode08-m_{domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.cnode09-m_{domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.cnode10-m_{domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.cnode11-m_{domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.cnode12-m_{domain}.cpu.*.cpu.idle.value)'

cnodes_cpu_wait:
    hideLegend: false
    target:
    - 'averageSeries(collectd.cnode01-m_{domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.cnode02-m_{domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.cnode03-m_{domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.cnode04-m_{domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.cnode05-m_{domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.cnode06-m_{domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.cnode07-m_{domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.cnode08-m_{domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.cnode09-m_{domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.cnode10-m_{domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.cnode11-m_{domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.cnode12-m_{domain}.cpu.*.cpu.wait.value)'

cnodes_memory_used:
    hideLegend: false
    target:
    - 'collectd.cnode*-m_{domain}.memory.memory.used.value'

cnodes_df_root_used:
    hideLegend: false
    target:
    - 'collectd.cnode*-m_{domain}.df.df.root.used'

cnodes_sda_disk_ops_read:
    hideLegend: false
    target:
    - 'collectd.cnode*-m_{domain}.disk.sda.disk_ops.read'

cnodes_sda_disk_ops_write:
    hideLegend: false
    target:
    - 'collectd.cnode*-m_{domain}.disk.sda.disk_ops.write'

snodes_load:
    yMin: 0
    yMax: 8
    hideLegend: false
    target:
    - 'collectd.snode*-m_{domain}.load.load.shortterm'

snodes_cpu_idle:
    yMin: 0
    yMax: 100
    hideLegend: false
    target:
    - 'averageSeries(collectd.snode01-m_{domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.snode02-m_{domain}.cpu.*.cpu.idle.value)'

snodes_cpu_wait:
    hideLegend: false
    target:
    - 'averageSeries(collectd.snode01-m_{domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.snode02-m_{domain}.cpu.*.cpu.wait.value)'

snodes_memory_used:
    hideLegend: false
    target:
    - 'collectd.snode*-m_{domain}.memory.memory.used.value'

snodes_df_root_used:
    hideLegend: false
    target:
    - 'collectd.snode*-m_{domain}.df.df.root.used'

snodes_drbd0_disk_ops_read:
    hideLegend: false
    target:
    - 'collectd.snode*-m_{domain}.disk.drbd0.disk_ops.read'

snodes_drbd0_disk_ops_write:
    hideLegend: false
    target:
    - 'collectd.snode*-m_{domain}.disk.drbd0.disk_ops.write'

mnodes_load:
    yMin: 0
    yMax: 8
    hideLegend: false
    target:
    - 'collectd.mnode*-m_{mgmt_domain}.load.load.shortterm'

mnodes_cpu_idle:
    yMin: 0
    yMax: 100
    hideLegend: false
    target:
    - 'averageSeries(collectd.mnode02-m_{mgmt_domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.mnode03-m_{mgmt_domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.mnode04-m_{mgmt_domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.mnode05-m_{mgmt_domain}.cpu.*.cpu.idle.value)'
    - 'averageSeries(collectd.mnode06-m_{mgmt_domain}.cpu.*.cpu.idle.value)'

mnodes_cpu_wait:
    hideLegend: false
    target:
    - 'averageSeries(collectd.mnode02-m_{mgmt_domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.mnode03-m_{mgmt_domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.mnode04-m_{mgmt_domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.mnode05-m_{mgmt_domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.mnode06-m_{mgmt_domain}.cpu.*.cpu.wait.value)'

mnodes_memory_used:
    hideLegend: false
    target:
    - 'collectd.mnode*-m_{mgmt_domain}.memory.memory.used.value'

mnodes_df_root_used:
    hideLegend: false
    target:
    - 'collectd.mnode*-m_{mgmt_domain}.df.df.root.used'

mnodes_sda_disk_ops_read:
    hideLegend: false
    target:
    - 'collectd.snode*-m_{mgmt_domain}.disk.sda.disk_ops.read'

mnodes_sda_disk_ops_write:
    hideLegend: false
    target:
    - 'collectd.snode*-m_{mgmt_domain}.disk.sda.disk_ops.write'

# cnode dashboard
cnode{num}_load:
    target:
    - 'collectd.cnode{num}-m_{domain}.load.load.*'

cnode{num}_cpu_used:
    areaMode: stacked
    target:
    - 'averageSeries(collectd.cnode{num}-m_{domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.cnode{num}-m_{domain}.cpu.*.cpu.user.value)'
    - 'averageSeries(collectd.cnode{num}-m_{domain}.cpu.*.cpu.system.value)'

cnode{num}_memory_used:
    target:
    - 'collectd.cnode{num}-m_{domain}.memory.memory.used.value'

cnode{num}_swap_used:
    target:
    - 'collectd.cnode{num}-m_{domain}.swap.swap.used.value'

# snode dashboard
snode{num}_load:
    target:
    - 'collectd.snode{num}-m_{domain}.load.load.*'

snode{num}_cpu_used:
    areaMode: stacked
    target:
    - 'averageSeries(collectd.snode{num}-m_{domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.snode{num}-m_{domain}.cpu.*.cpu.user.value)'
    - 'averageSeries(collectd.snode{num}-m_{domain}.cpu.*.cpu.system.value)'

snode{num}_memory_used:
    target:
    - 'collectd.snode{num}-m_{domain}.memory.memory.used.value'

snode{num}_swap_used:
    target:
    - 'collectd.snode{num}-m_{domain}.swap.swap.used.value'

# mnode dashboard
mnode{num}_load:
    target:
    - 'collectd.mnode{num}-m_{mgmt_domain}.load.load.*'

mnode{num}_cpu_used:
    areaMode: stacked
    target:
    - 'averageSeries(collectd.mnode{num}-m_{mgmt_domain}.cpu.*.cpu.wait.value)'
    - 'averageSeries(collectd.mnode{num}-m_{mgmt_domain}.cpu.*.cpu.user.value)'
    - 'averageSeries(collectd.mnode{num}-m_{mgmt_domain}.cpu.*.cpu.system.value)'

mnode{num}_memory_used:
    target:
    - 'collectd.mnode{num}-m_{mgmt_domain}.memory.memory.used.value'

mnode{num}_swap_used:
    target:
    - 'collectd.mnode{num}-m_{mgmt_domain}.swap.swap.used.value'

